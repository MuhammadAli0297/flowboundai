---
// Quality Monitoring hero background: "Inspection Line", ocean-palette, canvas 2D (see
// src/scripts/qualityMonitoringHero.ts). A horizontal conveyor runs through a fixed shield-check
// checkpoint; most batches cross straight through with a checkmark flash, one in five stops,
// pulses an alert ring and a flag mark, then continues on marked. The hub does not rotate.
---

<div class="hero-canvas-wrap absolute inset-0 overflow-hidden" aria-hidden="true">
  <div class="hero-base-gradient absolute inset-0"></div>
  <canvas id="quality-monitoring-hero" class="absolute inset-0 h-full w-full"></canvas>
  <div class="absolute inset-0 bg-grain opacity-20 mix-blend-overlay"></div>
</div>

<style>
  .hero-base-gradient {
    background:
      radial-gradient(85% 70% at 78% 50%, rgba(74, 111, 165, 0.26), transparent 60%),
      radial-gradient(70% 60% at 64% 85%, rgba(79, 109, 122, 0.22), transparent 60%),
      #104866;
  }
</style>

<script>
  import "../scripts/qualityMonitoringHero"
</script>
