<section id="cta" class="relative overflow-hidden bg-ocean-100 py-20 font-amulya md:py-24">
  <!-- a quiet pulse behind the button row, not the whole section: draws the eye to the one thing
       this section wants you to do, without competing with the copy above it -->
  <div class="pointer-events-none absolute inset-0 flex items-end justify-center pb-[6.5rem] md:pb-[7rem]" aria-hidden="true">
    <span class="cta-pulse"></span>
  </div>

  <div class="container-fb relative text-center">
    <h2 class="reveal-item mx-auto max-w-xl font-amulya text-[1.9rem] font-semibold leading-tight tracking-tight text-ocean-950 md:text-[2.3rem]" data-reveal>
      We're onboarding a small group of business pilots right now.
    </h2>
    <p class="reveal-item mx-auto mt-4 max-w-md text-[0.98rem] font-normal leading-relaxed text-ocean-950" data-reveal style="transition-delay: 90ms">
      Tell us a bit about your supply chain and we'll get back to you within a few days. No sales pressure, just a
      real conversation about how we can help your team run tighter and smarter.
    </p>
    <div class="reveal-item mt-9 flex flex-wrap items-center justify-center gap-4" data-reveal style="transition-delay: 180ms">
      <a
        href="mailto:hello@flowbound.ai?subject=Pilot%20request"
        class="group relative inline-flex items-center gap-2 overflow-hidden rounded-md border border-ocean-700 bg-ocean-700 px-6 py-3.5 font-semibold text-fb-white transition-all duration-300 ease-out hover:-translate-y-0.5 hover:shadow-[0_12px_30px_-10px_rgba(79,109,122,0.45)]"
      >
        <span aria-hidden="true" class="absolute inset-0 origin-left scale-x-0 bg-ocean-600 transition-transform duration-500 ease-out group-hover:scale-x-100" />
        <span class="relative z-10">Request a pilot</span>
        <span aria-hidden="true" class="relative z-10 transition-transform duration-300 ease-out group-hover:translate-x-1">&rarr;</span>
      </a>
      <a
        href="mailto:hello@flowbound.ai?subject=Question"
        class="group relative inline-flex items-center gap-2 overflow-hidden rounded-md border border-ocean-700 px-6 py-3.5 font-medium text-ocean-950 transition-all duration-300 ease-out hover:-translate-y-0.5"
      >
        <span aria-hidden="true" class="absolute inset-0 bg-ocean-700/10 opacity-0 transition-opacity duration-300 ease-out group-hover:opacity-100" />
        <span class="relative z-10">Talk to us</span>
        <span aria-hidden="true" class="relative z-10 transition-transform duration-300 ease-out group-hover:translate-x-1">&rarr;</span>
      </a>
    </div>
    <p class="reveal-item mt-6 font-mono text-xs uppercase tracking-[0.15em] text-ocean-950" data-reveal style="transition-delay: 270ms">hello@flowbound.ai</p>
  </div>
</section>

<style>
  .cta-pulse {
    width: 22rem;
    height: 5rem;
    border-radius: 9999px;
    background: radial-gradient(ellipse, rgba(74, 111, 165, 0.22) 0%, transparent 72%);
    animation: cta-pulse-breathe 3.4s ease-in-out infinite;
  }

  @keyframes cta-pulse-breathe {
    0%, 100% {
      opacity: 0.6;
      transform: scale(1);
    }
    50% {
      opacity: 1;
      transform: scale(1.12);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .cta-pulse {
      animation: none;
    }
  }
</style>

<script>
  import "../scripts/scrollReveal"
</script>
